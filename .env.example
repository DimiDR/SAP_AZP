# Optional local secrets (gitignored). Copy to app/azp-workschedulerule/.env
# Used mainly for deploy tasks with env:VAR references in ui5-deploy.yaml.
# For local npm start, prefer auth in ui5.yaml (local only) or OS credential store.

# Example for deploy:
# XYZ_USER=
# XYZ_PASSWORD=
